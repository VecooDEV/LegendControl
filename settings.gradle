pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net/' }
    }
}

rootProject.name = project_name

include 'forge12', 'forge12-defender', 'forge16', 'forge16-defender'